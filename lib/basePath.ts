// Must match `basePath` in next.config.ts. Client-side fetch() calls don't
// get this prefix automatically (unlike next/link), so it has to be added manually.
export const BASE_PATH = '/screening-bot-dashboard';
